import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'velocity6097.fuelmaster.dev',
  appName: 'FuelMaster',
  webDir: 'build'
};

export default config;
